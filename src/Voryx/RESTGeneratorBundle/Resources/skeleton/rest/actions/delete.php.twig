
    /**
     * Delete a {{ entity }} entity.
     *
     * @Delete("/{{ route_prefix }}/{id}", requirements={"id" = "\d+"})
     * @View(statusCode=204)
     * @Secure(roles="{{ roles['delete'] }}")
     *
     * @param Request $request
     * @param $entity
     *
     * @return Response
     */
    public function deleteAction(Request $request, {{ entity }} $entity)
    {
		return $this->delete($request, $entity);
    }
